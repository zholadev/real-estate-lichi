/**
 * @author Zholaman Zhumanov
 * @created 13.12.2023
 * @param page
 * @param event
 * @param error
 */
export const errorHandler = (page, event, error) => {
    return console.error(page, event, error)
}
